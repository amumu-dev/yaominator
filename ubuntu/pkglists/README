#!/bin/bash
#This is direcotry for my installed package list backup. Those files are generated by this command 
dpkg --get-selections > `date +%Y%m%d`.list


# it can be recovered by this 
#sudo dpkg --set-selections < /etc/package.selections && apt-get dselect-upgrade
